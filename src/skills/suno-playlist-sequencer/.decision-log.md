---
skill: suno-playlist-sequencer
classification: Simple Workflow (one carved reference)
customizable: yes (minimal [workflow] block)
headless: yes
---

# Decision Log — suno-playlist-sequencer

## Session 2026-06-18 — Build via extraction from suno-feedback-elicitor

### Why this skill exists

Created by extracting the album/playlist-sequencing apparatus out of `suno-feedback-elicitor`, which is now single-song scope only. This realizes the module's v2 principle — the lean agent (Mac) orchestrates; each workflow owns exactly one job. The feedback elicitor's own decision log (`suno-feedback-elicitor/.decision-log.md`, 2026-06-17 entry) carried this forward as a standing recommendation: the album apparatus (~13k tokens of methodology + the librosa album scripts) was off-mission for a single-song feedback skill, was left physically in place but UNROUTED, and was flagged for extraction into a dedicated `suno-playlist-sequencer` workflow. The smoke tests added to the two album scripts at that time gave this move a known-good baseline.

### What moved (git mv, history preserved where tracked)

- `references/playlist-sequencing-methodology.md` ← `suno-feedback-elicitor/references/` (git mv; ~21KB — kept as a carved reference because it's large and judgment-dense; correct to carve per build-process).
- `scripts/playlist-sequencing-data.py` ← `suno-feedback-elicitor/scripts/` (git mv).
- `scripts/batch-full-analysis.py` ← `suno-feedback-elicitor/scripts/` (git mv).
- `scripts/tests/test-playlist-sequencing-data.py` and `test-batch-full-analysis.py` ← `suno-feedback-elicitor/scripts/tests/` (these two were untracked / never committed, so `git mv` was not possible — moved with plain `mv`; no history to lose). Their "pending extraction / not routed" docstrings were rewritten now that extraction is complete.
- `scripts/tests/conftest.py` — copied (the standard hyphenated-test-collector pattern) so `test-*.py` files collect under pytest.

### Path decisions

- **sys.path depth unchanged.** Both moved scripts insert `Path(__file__).resolve().parent.parent.parent / "_shared"` (scripts → skill-root → src/skills → `_shared`). The new skill sits at the same depth as the old one (`src/skills/<skill>/scripts/`), so the `_shared` imports (`audio_deps`, `companion_writer`, `json_archiver`) resolve unchanged — verified by `--help` running clean. `_shared` stays shared; no copy made.
- **Cross-skill references in the methodology.** The methodology depends on `gemini-audio-analysis.md` (Camelot/felt-BPM foundation) and `audio-deep-analysis.py`, both of which legitimately STAY in `suno-feedback-elicitor` (they serve single-song deep analysis too). Those in-file references were rewritten from bare/same-dir form to "in the `suno-feedback-elicitor` skill's `references/` directory" plain-text pointers, since a bare path can't reach a sibling skill.

### Shared editor reference relocated (#4)

`STUDIO-EDITOR-REFERENCE.md` moved `suno-agent-band-manager/references/` → `_shared/references/` (git mv; new dir created) so it's genuinely module-shared rather than owned by one skill. All four live referrers retargeted: agent `README.md` (tree entry removed + relocation note), agent `SUNO-REFERENCE.md`, band-profile `tier-features.md` (×2 links), feedback `suno-parameter-map.md`. The lone remaining non-`_shared` mention is inside `suno-band-profile-manager/.decision-log.md` (immutable historical record — left as-is).

### Routing rewired

The agent (Mac) now ROUTES playlist/album/tracklist work to this skill instead of doing it inline — mirroring how it routes to its other orchestrated sub-skills. Updated: agent `capabilities.md` (added `suno-playlist-sequencer` to External Skills with an Expected-return line; removed the 2 moved scripts from the Feedback Elicitor's audio-script list; converted the "For playlist work" block to a route-don't-sequence-inline block), agent `creed.md` (methodology pointer + Multi-Band Discipline script attribution), agent `USAGE.md` (Playlist Sequencing section). The Feedback Elicitor `SKILL.md` / `references/gemini-audio-analysis.md` / `references/README.md` now carry a one-line route-to-`suno-playlist-sequencer` pointer so they're not dead ends. Band-profile `profile-schema.md` retargeted (allowed: references/ only — band-profile SKILL.md/scripts/tests/decision-log left untouched, owned by another agent).

### Skill shape choices

- **Simple Workflow, single SKILL.md + one carved reference.** The orchestration (intake → run script → apply methodology → present) fits inline as named sections; the only carve-out is the large methodology doc. Did not over-split.
- **Decision-Log Workspace, light touch.** A recommended sequence is a revisable artifact (re-sequence after regen waves is the common case), so the pattern applies — but treated in ~3 sentences per the principles file's Treatment style: workspace at `docs/{band-slug}-playlist-sequencing/`, decision log as canonical memory, read-decisions-first on re-sequence, audit at handoff. No `## Workspace` header, no tree diagram, no prescribed frontmatter. Did NOT add `output_dir`/`output_folder_name` scalars — the task specified a minimal `[workflow]` block (prepend/append/persistent_facts only), so the workspace path is bound inline to a stable per-band path rather than a customizable scalar.
- **customize.toml** is the minimal canonical block (`activation_steps_prepend=[]`, `activation_steps_append=[]`, `persistent_facts=["file:{project-root}/**/project-context.md"]`), with the resolver as On-Activation step 1 — mirrors the `suno-style-prompt-builder` sibling.
- **Headless** supported with the standard return discipline: success carries `recommended_sequence`, `locked_arcs_respected`, `trade_offs`, `artifacts`, and a `decision_log[]` of assumptions; blocked carries `missing` + one-line `reason`. Headless must read songbooks itself for thematic claims (or omit them and log the omission), since it can't pause for the interactive Thematic-Verification reads.

### Registration

- Symlink `.claude/skills/suno-playlist-sequencer -> ../../src/skills/suno-playlist-sequencer/` created (matches the relative form of sibling suno-* symlinks).
- Appended a row to the source asset `src/skills/suno-setup/assets/module-help.csv` (menu-code `PS`, display "Sequence Playlist", after `suno-feedback-elicitor:elicit-feedback`), mirroring the existing append-a-row pattern. The active `_bmad/module-help.csv` is regenerated by running `suno-setup` — NOT hand-edited here (per guardrails / setup-owned). Module version in `suno-setup/assets/module.yaml` left unbumped to avoid colliding with another agent.

### Validation

- Path-standards lint: PASS (0 findings). Scripts lint: PASS (0 findings, 2 python scripts, 0 missing tests).
- Tests: 25 passed across `_shared/tests` + this skill + `suno-feedback-elicitor/scripts/tests`; the 2 moved smoke tests collect via conftest. Both moved scripts run clean via `--help` (confirms `_shared` imports resolve from the new root).
- Dangling-reference grep (excluding `.analysis/`, `src/skills/reports/`, and `.decision-log.md` historical records): ZERO. No `STUDIO-EDITOR-REFERENCE` outside `_shared`, no methodology/script reference pointing back into `suno-feedback-elicitor`.
## 2026-06-18 — Quality analysis (post-v2-remediation re-run)

Grade: Good. HTML: `.analysis/20260618-091127/quality-report.html`, markdown: `.analysis/20260618-091127/quality-report.md`.

## 2026-06-18 — Quality-opportunity remediation pass

Resolved the open quality-analysis opportunities from the run above. Edits confined to files under `src/skills/suno-playlist-sequencer/`.

### OPP-high — Methodology re-homed for standalone life (`references/playlist-sequencing-methodology.md`)
- **Locked-arc section:** replaced "never break a documented locked arc on Mac's own authority" → "on your own authority". Replaced the Lenny-specific locked-arc paragraph (four-song Love & Loss / Heal sequence + `session-context-for-mac.md` citation) with a generic illustration (a writer-positioned love→loss→grief→healing run flagged in the playlist YAML/by the user). The methodology no longer assumes the agent's private context.
- **"Mac discipline" → "Thematic-verification discipline":** renamed the heading and rewrote all four bullets to "you"/"the agent" so the discipline survives standalone.
- **`{communication_language}`/`{document_output_language}` anchor:** added at the Process output point — proposal narrative in `{communication_language}`, persisted companion/proposal doc in `{document_output_language}`.
- **Cross-References:** updated the two stale non-per-band paths to the per-band forms used everywhere else (`docs/audio-analysis/playlists/{band-slug}.json`, `docs/{band-slug}-playlist-sequencing.md`). This also subsumes OPP-low `:218`.
- Softened the one remaining standalone-unsafe generalization ("Lenny in particular uses paradox-as-structure…") to a writer-agnostic statement. The documented-example bullets (The Slide, Distant Mourning, etc.) were KEPT — they are the load-bearing evidence for why surface inference inverts, and serve as concrete domain framing rather than agent-context dependencies.

### OPP-medium — Silent track-drop on missing/mistyped filename
- SKILL.md Step 2: added a track-count reconciliation block — compare analyzed count vs. YAML entry count, surface MISSING/errored tracks BY NAME and confirm before sequencing; headless records each in `dropped_tracks[]`.
- SKILL.md headless success contract: added `dropped_tracks[]` field (behavioral + one contract field; no script change).

### OPP-medium — Regen-wave + catalog reads + headless prose
- SKILL.md Step 2: re-eval-after-regen now diffs fresh script output against the prior archive and leads the proposal with what moved / which transitions degraded.
- methodology Process step 4: catalog-scale songbook reads delegate the full read to a subagent that returns ONLY a compact per-song theme summary; the parent-read is reserved for the 1-2-track case.
- SKILL.md headless contract: added `flagged_transitions[]`, and extended the `decision_log[]` note to require entries for declined locked-arc breaks and felt-BPM-on-faith sequencing.

### OPP-low — Stale companion path
- `scripts/playlist-sequencing-data.py` `--companion` help string: aligned to the per-band `docs/{band-slug}-playlist-sequencing.md` form that `resolve_companion_path` actually produces. (Methodology Cross-References handled under OPP-high.) The `--archive` help string's `<album>.json` was left as-is — out of the named scope and substantively correct (the archiver slugifies the album name).

### OPP-low — Facilitative seams / degenerate cases (the cheap high-value ones)
- SKILL.md Step 4: capture-don't-interrupt — route mid-flow narrative asides into the decision log.
- SKILL.md Step 4: soft-gate before presenting ("anything fixed I should respect before I commit?").
- SKILL.md Step 1: scope/expectation framing in intake (say back the depth before working).
- SKILL.md Step 1: tiny-playlist (≤4 tracks) degenerate-case guard (no album-arc geography to shape).
- SKILL.md Step 2: named the fully-degraded sub-case (deps gone AND no prior archive) — no audio data; don't fabricate numbers; proceed narrative-only and flag.
- SKILL.md Step 3: added the parallel sonic-vs-narrative review-lens for full re-sequences (cheap, high-value per the principles file's "Parallel review lenses" pattern).

### Skipped (with reason)
- **Yolo quick-win mode (OPP-low, lower priority):** SKIPPED — not cheap. A Yolo mode is a third activation mode with its own contract/intake shape; the build decision log already recorded that the three-mode architecture wasn't fully adopted here. Adding it is a design decision beyond a remediation pass, so deferred rather than bolted on.

### Validation
- scan-path-standards.py: no findings in any edited file (SKILL.md, methodology, script). The 15 raw findings are all pre-existing, confined to `.decision-log.md` historical records and `.analysis/` prior-run artifacts (both excluded surfaces).
- scan-scripts.py: PASS (0 findings, 2 python scripts, 0 missing tests).
- Tests: 26 passed (`src/skills/_shared/tests` + `src/skills/suno-playlist-sequencer`). Both scripts run clean via `--help`; the new `--companion` string shows the per-band form.

