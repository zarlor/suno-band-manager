# Customization overrides for suno-lyric-transformer.
# Teams/users override without editing this file:
#   {project-root}/_bmad/custom/suno-lyric-transformer.toml       (team, committed)
#   {project-root}/_bmad/custom/suno-lyric-transformer.user.toml  (personal, gitignored)
# Merge order: this file -> team -> user. Scalars override; plain arrays append.

[workflow]
activation_steps_prepend = []
activation_steps_append = []
persistent_facts = ["file:{project-root}/**/project-context.md"]
