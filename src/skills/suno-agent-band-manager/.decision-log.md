# Decision Log — suno-agent-band-manager (Mac)

## 2026-06-18 — Phase 1: v2 hygiene + agent_type declaration

Phase 1 of the v2 migration for the "Mac" agent. Scope was strictly hygiene +
a metadata file: no sanctum/memory restructure, no activation-logic changes, no
creed sharding, no edits to the live `_bmad/` memory store. Driven by the lint
findings in `.analysis/20260618-100544/` (`path-standards-temp.json`,
`scripts-temp.json`) and the recommended `[agent]` block from
`customization-surface-analysis.md`.

### 1. Path hygiene (44 path-standards findings → 4 accepted intrinsic)

- **Cross-directory `./` references (37 findings).** Replaced every
  cross-directory `./references/…` and `./scripts/…` with bare skill-root-relative
  paths (`references/…`, `scripts/…`) per `standard-fields.md` "Cross-Directory
  References". Files touched: `SKILL.md`, `references/activation.md`,
  `references/creed.md`, `references/create-song.md`, `references/init.md`,
  `references/memory-system.md`, `references/refine-song.md`,
  `references/save-memory.md`.
- **Bare `_bmad` references (3 findings).** Prefixed two genuine path references
  with `{project-root}/`: `references/USAGE.md:653` (sidecar chronology path) and
  `references/reconcile.md:32` (`_bmad/config*.yaml`). The third —
  `references/memory-system.md:85` — was **left as-is** and is **accepted**: the
  bare `_bmad/_memory/band-manager-sidecar/` there is a documentation *example*
  inside a sentence that explicitly explains the access-boundaries bare-relative
  convention ("no `{project-root}/` placeholder"). Prefixing it would make the
  prose contradict itself.
- **Home-directory path (1 finding).** `references/memory-system.md:44` — changed
  the illustrative Claude Code cache path from `~/.claude/projects/...` to the
  portable `$HOME/.claude/projects/...` form.
- **Parent-directory `../` cross-skill links (3 findings) — ACCEPTED, left in
  place.** These are intrinsic cross-skill orchestration links, not fragile
  intra-skill paths. Each target was verified to exist:
  - `references/README.md:126` → `../../_shared/references/STUDIO-EDITOR-REFERENCE.md` (exists)
  - `references/SUNO-REFERENCE.md:3` → `../../_shared/references/STUDIO-EDITOR-REFERENCE.md` (exists)
  - `references/creed.md:298` → `../../suno-style-prompt-builder/references/model-prompt-strategies.md` (exists)

  Final live path scan (excluding `.analysis/`): only these 4 accepted findings
  remain (3 cross-skill `../` + 1 documentation-example `_bmad`).

### 2. Conventions block

Stamped the canonical 4-line `## Conventions` block (from the agent-builder
bootloader template) into `SKILL.md` between "The Sacred Truth" and
"On Activation". Adds 6 lines; the bootloader stays lean, so a pointer-note was
not needed.

### 3. Script hygiene (14 scripts findings → 0 live)

- **PEP 723 inline dependency blocks added:** `genre-coverage.py` (stdlib-only),
  `reconcile-sidecar.py` (stdlib-only), `regenerate-index-sections.py`
  (`pyyaml>=6.0`), `validate-sidecar.py` (`pyyaml>=6.0`). `pipeline-guard.py`
  already had one.
- **Replaced `pip install` references** in `regenerate-index-sections.py` and
  `validate-sidecar.py` import-error messages with `uv run` / PEP 723 guidance.
- **Structured JSON output (`--format json` via `json.dumps`)** added to
  `genre-coverage.py` and `regenerate-index-sections.py`; `validate-sidecar.py`,
  `reconcile-sidecar.py`, and `pipeline-guard.py` already emitted structured JSON.
- **argparse `--help`** added to `pipeline-guard.py` (Stop hook; reads payload
  from stdin, args added purely for self-documentation/graceful-degradation).
- **Unit tests added** for the 5 previously-untested management scripts under
  `scripts/tests/` (`test-genre-coverage.py`, `test-pipeline-guard.py`,
  `test-reconcile-sidecar.py`, `test-regenerate-index-sections.py`,
  `test-validate-sidecar.py`), mirroring the existing hyphenated `test-*.py`
  style and `spec_from_file_location` module-loading pattern.
- **Added `scripts/tests/conftest.py`** (matching the sibling skills') with a
  `pytest_collect_file` hook so hyphenated `test-*.py` files are collected.
  Without it, pytest's default `test_*.py` glob collected zero tests.
- **Deleted committed on-disk `__pycache__` dirs** under `scripts/` and
  `scripts/tests/` (gitignored via `.gitignore:1`).

### 4. "Schizo" fix

`references/refine-song.md:109` — the Step 4 output template header read
`## Schizo Refinement Update`. "Schizo" is a real published Solitary Fire track,
not a placeholder, so the template would literally emit it for any song.
Replaced with the neutral placeholder `## {band-name} Refinement Update`.
Confirmed no other "Schizo" occurrences in skill code/scripts (remaining hits are
only in `.analysis/`, out of scope).

### 5. agent_type declaration (`customize.toml`)

Created `customize.toml` next to `SKILL.md` with a **metadata-only `[agent]`
block** (NOT `[workflow]`), per `standard-fields.md`:
`code = "suno-agent-band-manager"`, `name = "Mac"`, `title = "Band Manager"`,
`icon = "🎺"`, `description = "Orchestrates Suno song package creation."`
(SKILL.md's first sentence), `agent_type = "memory"`.

Deliberately **omitted** any override scalars (`activation_steps_*`,
`persistent_facts`) and any persona/creed/menu fields — for a memory agent the
sanctum (sidecar + persona/creed + voice-context + mac-preferences) is the
behavior-customization surface, and the override surface stays empty. `name` is
the authored seed "Mac" (fixed in persona/creed, not First-Breath-learned), so an
explicit name is the honest declaration rather than `""`.

### Validation

- `scan-path-standards.py`: live findings (excluding `.analysis/`) = 4, all the
  accepted intrinsic ones above.
- `scan-scripts.py`: status `pass`, 0 live findings.
- Test suite (`pytest -q`, `--with pytest --with pyyaml`): **66 passed**
  (3 pre-existing + 63 new across the 5 added test files).

## 2026-06-18 — Phase 2: four additive enhancements (mission line, save-as-you-go, headless branch, genre-coverage wiring)

Phase 2 of the v2 migration for the "Mac" agent. Scope was strictly **additive** —
four enhancements from `.analysis/20260618-100544/` (the
`enhancement-opportunities-analysis.md` findings H1/H2 and the structure analysis).
**No** memory/sanctum restructure, no `index.md` rename, no memory-script rewiring,
no edits to the live `_bmad/` store, no creed sharding. All four items leave Mac
fully working; nothing existing was rewritten, only added to / wired up.

### 1. Species mission line (SKILL.md + creed.md)

- **`SKILL.md`** — added a one-line `**Why Mac exists:**` sentence directly under
  the identity seed (between the seed paragraph and The Three Laws): *"to take the
  owner's creative spark and hand back a Suno-ready package they couldn't have
  assembled alone — that's the whole gig."* Bootloader stays lean (one line added).
- **`references/creed.md`** — echoed it as a new top-of-file `## Mission` section
  (2 lines) above `## Principles`, framing every discipline below as in service of
  getting the package right and never losing the work. Refined from the analysis's
  suggested wording to fit Mac's voice.

### 2. Save-as-you-go in First Breath (`references/init.md`)

Added a **"Save as you go — don't wait for the end."** paragraph to the Progressive
Preference Discovery section (after step 4, before tier-discovery). Mirrors the v2
resilience standard: write each discovered preference into `index.md` the moment
it's discovered, not in one end-of-setup batch; *"A setup that gets interrupted
keeps whatever you already saved."* Explicitly ties it to the creed's existing
don't-lose-work posture (Workshop Capture, Sync at the point of change) and to the
"born already-migrated" structure so the incremental writes always have a valid
file to land in. Fixes the inconsistency where Mac's onboarding batched preference
writes despite the rest of the agent being fanatical about not losing work.

### 3. Headless branch (`references/activation.md`)

SKILL.md's activation router advertised a "Headless" path that `activation.md`
never implemented. Added a **`## Headless Activation (non-interactive)`** section
(between the Full Activation Sequence and Mode Switching) that:
- Detects `--headless:{capability}`, `-H {capability}`, or a structured JSON
  payload naming a capability.
- Skips greeting/menu/sync-prompt/reconcile-Handoff-Checkpoint and the routed
  capability's interactive soft gates.
- **Still** loads config + `access-boundaries.md` (a headless run is bound by the
  same read/write/deny zones — "do not widen access just because no human is
  watching"), resolves the capability via `{routing_table}`, runs the capability's
  own headless contract (e.g. create-song.md "Headless Mode"), and **still persists**
  per the capability's normal flow (including `scripts/regenerate-index-sections.py`
  and `scripts/genre-coverage.py` where already called). Carve-out: Law-3
  never-overwrite-without-asking narrative writes are staged + reported as warnings,
  not auto-applied.
- Returns the structured result
  `{status: complete|blocked, capability, artifact_path, summary, warnings[]}`,
  with a one-line reason on `blocked`, an unknown-capability case, and a
  "never fabricate an artifact path" guard.

### 4. Wire `genre-coverage.py` into the publish path + Catalog Verification (creed.md, save-memory.md, create-song.md)

`scripts/genre-coverage.py` was fully built/tested but referenced nowhere. Wired in
using its **actual CLI** (verified by running it against the live repo —
`docs/{band}-genre-coverage.md` outputs are gitignored, so the verification run left
no tracked changes):

```
python3 scripts/genre-coverage.py "{project-root}" [--band {band-slug}] --timestamp "{today's date}" [--format json]
```

- **`references/save-memory.md`** — added **step 4a-bis** right after the
  `regenerate-index-sections.py` step (4a): when the session published a song /
  changed a band's recorded territory, run `genre-coverage.py` to regenerate
  `docs/{band}-genre-coverage.md`. Notes the AUTOGEN-bounded rewrite, the `--band`
  single-band option, `--format json` summary, and the clean-exit-on-empty-catalog
  case.
- **`references/create-song.md`** — added a bullet to the Step 7 "sync at each
  sub-step write" list: adding a songbook entry that changes recorded territory →
  regenerate that band's coverage index (`--band {band-slug}`) in the same publish
  batch. Cross-references save-memory.md step 4a-bis.
- **`references/creed.md`** (Catalog Verification Discipline) — rewrote the
  "Mechanical pre-check step" so Mac **reads the generated
  `docs/{band-slug}-genre-coverage.md` index FIRST** (regenerating it on the spot if
  stale/missing), and falls back to the manual multi-term grep **only if the script
  is unavailable**. Updated the "How to apply" first-step bullet and the
  end-of-section "Self-check before asserting" line to match. The manual grep is now
  explicitly the backstop, not the primary path — directly addressing the two
  documented confidence-from-memory failures (piano-led-as-new-LV, LSNM
  groove-metal-as-fresh).

### Validation

- `scan-path-standards.py`: **no new findings introduced.** Live findings excluding
  `.analysis/` and `.decision-log.md` = the same **4 accepted intrinsic** ones from
  Phase 1 (3 cross-skill `../` links in README.md / SUNO-REFERENCE.md / creed.md +
  1 bare-`_bmad` documentation example in memory-system.md). The single finding in a
  Phase-2-edited file is `creed.md:308` — the pre-existing accepted
  `../../suno-style-prompt-builder/...` cross-skill link, shifted down from :298 by
  the added Mission + Catalog-Verification content; not a new finding. All paths I
  added use bare skill-root form (`scripts/…`, `docs/…`, `references/…`).
- Re-read each edited file end-to-end for coherence and Mac's voice.

## 2026-06-18 — Phase 3a: v2 sanctum scaffolding + READ-ONLY migration tool

Phase 3a builds the v2 sanctum scaffolding (templates + a fresh-init script) and a
NON-DESTRUCTIVE data-migration tool that stages a v2 sanctum from the live sidecar.
**This phase does not cut over.** The live sidecar at
`{project-root}/_bmad/_memory/band-manager-sidecar/` was READ ONLY — confirmed
byte-identical (sha256) before and after every dry-run. No cutover, no rewiring of the
8 existing memory scripts, no SKILL.md/activation.md restructure, no git commit.

### Adopt v2 vocabulary, preserve the superior bespoke machinery

- **Sanctum root stays at `_bmad/_memory/band-manager-sidecar/`** (double underscore),
  NOT the v2-default `_bmad/memory/{skill}/`. The path is load-bearing — portable-sync +
  reconcile tooling depend on it. **Preserved divergence**, documented in the INDEX/PULSE
  templates and both new scripts' module docstrings.
- **Two-tier memory split** introduced to bound the always-loaded size: the ~18
  "Current/Prior Work — Session …" narrative blocks move to a NEW raw layer
  `sessions/YYYY-MM-DD.md` (NOT loaded on rebirth); the non-session content (preferences,
  derived catalog sections, module state, pending/parked, session history) becomes a tight
  curated `MEMORY.md` (102 lines on the real data — well under the ~200 target).
- **Derived-section markers preserved verbatim.** `MEMORY.md` carries the
  `<!-- derived:recently-published:start/end -->` and `<!-- derived:catalog-status:start/end -->`
  marker pairs that `regenerate-index-sections.py` rewrites, so that script keeps working
  against the new file unchanged.
- **CREED sharded.** A slim always-load CORE (Mission, Principles, Three Laws, Sacred
  Truth, Package Assembly Rule **core**, Dominion pointer, shard map) + heavy disciplines
  in capability-scoped `creed-<discipline>.md` shards + verbose incident narratives in a
  NON-loaded `creed-incident-log.md`. The Three Laws, Sacred Truth, and Package Assembly
  Rule core are marked **INVARIANT constitution**. The Package Assembly Rule core stays in
  the always-loaded CORE because the root `CLAUDE.md` guarantees the creed loads every
  activation (the Suno Pipeline Rule there points back to it).
- **BOND.md is a thin orienting file**, not a single collapsed owner-model. Mac's
  owner-model is richer than v2's single BOND (split across `docs/voice-context-{user}.md`,
  `docs/mac-preferences.md`, sidecar `patterns.md`). BOND.md points to those richer
  traveling sources + a short owner summary; the split is preserved, not collapsed.
- **`access-boundaries.md` preserved** as its own loaded-first Dominion file (stronger than
  a generic CREED-Dominion section). `patterns.md`, `chronology.md`, `_collection_*.txt`
  preserved as organic / working files (copied byte-identical into staging, referenced from
  INDEX).
- **PULSE.md** is a narrow maintenance autonomous wake: on wake run validate-sidecar +
  regenerate-index-sections (dry-run) + genre-coverage + check-memory-health, flag drift +
  oversized memory, REPORT-AND-STAGE for the next session, NEVER autonomously edit
  creative/song/lyric content (Law 3 hard line).

### Files created (non-destructive)

- `assets/INDEX-template.md`, `assets/PERSONA-template.md`, `assets/CREED-template.md`,
  `assets/BOND-template.md`, `assets/MEMORY-template.md`, `assets/PULSE-template.md` —
  with `{user_name}`, `{communication_language}`, `{birth_date}`, `{project_root}`
  placeholders per `sample-init-sanctum.py`.
- `scripts/init-sanctum.py` — fresh-sanctum scaffolder (adapted from
  `sample-init-sanctum.py`; SKILL_NAME + bespoke sanctum location; copies templates +
  substitutes; creates `sessions/` + `capabilities/`; generates CAPABILITIES.md). PEP 723.
- `scripts/migrate-sidecar-to-v2.py` — THE migration tool. READ-ONLY on the source; writes
  only under `--out` (default `_bmad/_memory/.sidecar-v2-staging`). Splits `index.md` →
  MEMORY.md (markers preserved) + `sessions/YYYY-MM-DD.md` (same-date blocks grouped);
  seeds PERSONA/CREED(+shards+incident-log)/BOND/CAPABILITIES/PULSE; copies the bespoke
  files byte-identical. `--verify` reports session counts, marker presence, char
  accounting, preserved-file integrity, and the file list. PEP 723.
- `scripts/tests/test-init-sanctum.py`, `scripts/tests/test-migrate-sidecar-to-v2.py` —
  unit tests using a small SYNTHETIC fixture sidecar (never the real data). The existing
  `conftest.py` (`pytest_collect_file` on `test-*`) collects them automatically.

### DRY-RUN against the REAL live sidecar (staged, then verified)

`uv run scripts/migrate-sidecar-to-v2.py --project-root . --out _bmad/_memory/.sidecar-v2-staging --verify`

- **status: pass**, zero findings.
- 18 session blocks → 14 session files (same-date grouping: 4 blocks on 2026-04-20 → 1
  file; 2 blocks on 2026-05-06 → 1 file).
- Both derived marker pairs present in MEMORY.md.
- Char accounting: all 26 source sections present verbatim. source_section_chars=144,184;
  output_chars=146,927 (the +2,743 delta is additive per-session-file headers, not dropped
  source — accounting confirms `all_sections_present: True`, `dropped_sections: []`).
- All 6 preserved files byte-identical. 31 files produced.
- **Live sidecar sha256 byte-identical before/after every dry-run — UNTOUCHED.**

### Validation

- `scan-scripts.py`: **clean** (0 findings) on the skill, including the two new scripts.
- `scan-path-standards.py`: **4 pre-existing accepted intrinsic findings unchanged**; the
  only NEW findings are 4 `{project_root}/_bmad/_memory/…` lines in INDEX/PULSE templates.
  These are intrinsic to the v2 `{project_root}` (underscore) substitution placeholder
  mandated by `sample-init-sanctum.py` — the scanner only recognizes the hyphenated
  `{project-root}/` prefix, so a substitution-target line using the underscore var trips
  the bare-`_bmad` check. Same class as the accepted `memory-system.md:85` finding.
  Avoidable documentation-only path tokens were reworded out; the 4 that remain are
  load-bearing substitution targets and cannot be changed without breaking either the v2
  placeholder contract or the documented `_bmad/_memory/` divergence. **Accepted.**
- Full test suite green: **85 passed** via
  `uv run --with pytest --with pyyaml python -m pytest src/skills/suno-agent-band-manager -q`
  (+19 from Phase 3a: 6 init-sanctum + 13 migration).

## 2026-06-18 — Phase 3b: rewire Mac to the v2 sanctum (cut-over NOT performed)

Phase 3b rewires the 5 memory scripts and the skill `.md` files to the v2 sanctum
layout staged/verified at `_bmad/_memory/.sidecar-v2-staging/`. **This phase does
NOT cut over** — the LIVE sidecar at
`{project-root}/_bmad/_memory/band-manager-sidecar/` was left byte-identical
(sha256 `dde7469e…` before and after, confirmed), the staging original was READ
only, and all rewired scripts were tested against a throwaway
`_bmad/_memory/.sidecar-test-copy` (created + deleted) via a new `--sanctum-dir`
override. No git commit. All edits confined to `src/skills/suno-agent-band-manager/`.

### Scripts rewired (file contract change + `--sanctum-dir` override)

Each script gained a `--sanctum-dir` (check-memory-health: takes precedence over
its positional sanctum-path) defaulting to the real
`{project-root}/_bmad/_memory/band-manager-sidecar/`, so they're testable against a
staging copy without touching live data. A `resolve_sanctum_dir()` helper was added
to each.

1. **`regenerate-index-sections.py`** — derived `recently-published` /
   `catalog-status` sections now rewritten in **`MEMORY.md`** (was `index.md`). Same
   marker pairs, same algorithm; only the target file + display keys changed
   (`index_path` → `memory_path`). Added `display_path()` so an out-of-root staging
   override doesn't break `relative_to`.
2. **`validate-sidecar.py`** — all **6 integrity checks preserved**. The two
   catalog cross-checks (`check_index_recently_published`,
   `check_index_catalog_counts`) now read the derived sections from **`MEMORY.md`**
   and report findings against its path; `run_checks()` takes `sanctum_dir`.
3. **`reconcile-sidecar.py`** — the "docs newer than the store" reference is now the
   **newest mtime of `MEMORY.md` / `INDEX.md`** (was `index.md`). Payload keys
   `index_path`/`index_mtime` → `store_path`/`store_mtime`; report header → "Sanctum
   Reconciliation Report"; passes `--sanctum-dir` through to the validator subprocess.
4. **`check-memory-health.py`** — new threshold model for the v2 store: `MEMORY.md`
   is **line-bounded (~200 lines)** — it's curated/bounded but its derived-catalog
   lines are legitimately long single lines, so a tight CHAR cap would false-alarm;
   the line bound is the honest one. `patterns.md` / `chronology.md` kept in the
   check with **generous char ceilings** (60k / 120k) sized to the real preserved
   organic files (NOT loaded on rebirth, so size has little runtime cost).
   `sessions/` files are reported as a count but **never flagged** (raw logs). The
   result is **GREEN on the migrated store** (real sizes: MEMORY 102 lines, patterns
   42.9k chars, chronology 93.8k chars — all under bound).
5. **`pre-activate.py`** — `--scaffold` now **delegates to `init-sanctum.py`**
   (full v2 sanctum from `assets/` templates) instead of writing the old 3 inline
   stubs. Output gained `sanctum_load_order`
   (`access-boundaries.md → INDEX.md → MEMORY.md → CREED.md → PERSONA.md`).
   `check_first_run` honors `--sanctum-dir`.

Unit tests updated/extended for the new layout across all 5 (MEMORY.md targets,
`--sanctum-dir` override tests, line-vs-char health tests, init-sanctum delegation
tests, sessions-not-flagged test). `scan-scripts.py`: **status pass, 0 findings**.

### Skill `.md` files restructured

- **`references/memory-system.md`** — rewritten as the canonical v2 memory doc: the
  file vocabulary, the two-tier `sessions/` → `MEMORY.md` model, the loaded /
  on-demand / not-loaded tiers, the preserved bespoke machinery (standalone
  access-boundaries, thin BOND, organic patterns/chronology/_collection), and the
  traveling companion files (voice-context, mac-preferences).
- **`references/activation.md`** — "sanctum exists" path now loads in order:
  `access-boundaries.md` FIRST → `INDEX.md` → `MEMORY.md` → `CREED.md` core →
  `PERSONA.md` → run pre-activate → reconcile gate (unchanged) → greet → menu. "No
  sanctum" path runs `init-sanctum.py` then First Breath. Headless branch made
  consistent (loads access-boundaries + CREED core). **New "Maintenance / Pulse
  Wake" branch** added: on autonomous wake, load `PULSE.md` and run its
  report-and-stage maintenance-only routine, never editing creative content (Law 3).
- **`SKILL.md`** — On Activation routing references the new files/scripts
  (init-sanctum.py for scaffold; the ordered sanctum load; Pulse wake). Bootloader
  stays lean; Three Laws / Sacred Truth / Mission / Conventions kept.
- **`references/save-memory.md`** — rewritten for the two-tier model: append raw to
  `sessions/YYYY-MM-DD.md`, distill into `MEMORY.md` (kept tight), regenerate derived
  sections (`regenerate-index-sections.py` → `MEMORY.md`) + genre-coverage, update
  `INDEX.md` when a new organic file is created. Milestone Auto-Save +
  sync-at-point-of-change preserved.
- **`references/init.md`** — First Breath aligned with `init-sanctum.py`: the script
  scaffolds from templates (born already-migrated with marker pairs); init.md is the
  conversational calibration AFTER scaffolding. Save-as-you-go (Phase 2) retained,
  now writing into the scaffolded `MEMORY.md`. The obsolete inline structure /
  `index.md` template / access-boundaries template blocks were removed.
- **`references/reconcile.md`, `references/USAGE.md`, `references/create-song.md`,
  `references/refine-song.md`** — `index.md` / "sidecar index" references updated to
  `MEMORY.md` / `INDEX.md` / "sanctum" as appropriate.

### CREED wiring (CLAUDE.md Package-Assembly-on-activation guarantee preserved)

- **Activation loads the SANCTUM `CREED.md` core, NOT the 358-line
  `references/creed.md`.** A note in `activation.md` step 4 and a top-of-file banner
  on `references/creed.md` make this explicit: `references/creed.md` is the authored
  SOURCE/template lineage (what the sanctum was seeded from) and is **not loaded on
  rebirth** — eliminating any double-load. The sanctum `CREED.md` core carries the
  **Package Assembly Rule core**, which the root `CLAUDE.md` guarantees loads on
  every activation (incl. headless). The Suno Pipeline Rule in root `CLAUDE.md` still
  points at the creed; that contract is intact because the core stays in the
  always-loaded sanctum `CREED.md`.
- **Capability-scoped shard loads wired:** `references/capabilities.md` gained a
  "Creed Shards — Load On Demand" table; `create-song.md` (Steps 3–5) and
  `refine-song.md` (Step 3) load `creed-package-assembly.md` on package paths;
  `create-song.md` Step 2 loads `creed-workshop-capture.md` when drafting and
  `creed-disciplines.md` when making catalog/thematic claims. `creed-incident-log.md`
  marked reference-only (not loaded).

### PULSE wiring

`SKILL.md` On Activation + `activation.md` "Maintenance / Pulse Wake" reference
`PULSE.md` as Mac's narrow maintenance wake (report-and-stage, never edits creative
content — Law 3 hard line). `memory-system.md` points at the activation Pulse branch.

### Test-copy validation (vs `_bmad/_memory/.sidecar-test-copy`, deleted after)

- **regenerate-index-sections** — derived rewrite into `MEMORY.md` works (dry-run
  matches songbook ground truth; write `regenerated`, re-run `unchanged`).
- **validate-sidecar** — `PASS`, **0 errors**, 68 informational `cross_reference_missing`
  warnings on unrelated `docs/RA/*` + `docs/mac-preferences.md` files (pre-existing
  content, not migration-related). The catalog cross-checks against `MEMORY.md`
  agreed with ground truth.
- **reconcile-sidecar** — `status: clean`, validator `pass`, 0 newer files,
  `needs_reconciliation: False`.
- **check-memory-health** — **GREEN** (`maintenance_recommended: False`,
  `needs_pruning: []`, 14 session files counted, not flagged).
- **pre-activate** — surfaces `sanctum_load_order`
  `[access-boundaries.md, INDEX.md, MEMORY.md, CREED.md, PERSONA.md]`.

### Validation

- `scan-path-standards.py`: **7 live findings, all pre-existing accepted intrinsic**
  (4 INDEX/PULSE-template `{project_root}/_bmad` substitution targets + 3 cross-skill
  `../` links in README/SUNO-REFERENCE/creed). The 3 transient new findings my
  `memory-system.md` rewrite introduced (documentation-example bare-`_bmad` tokens in
  the divergence blockquote) were reworded out — back to the accepted baseline. **No
  new findings.**
- `scan-scripts.py`: **status pass, 0 findings.**
- Full test suite: **92 passed** via
  `uv run --with pytest --with pyyaml python -m pytest src/skills/suno-agent-band-manager -q -p no:cacheprovider`.
- **LIVE sidecar `dde7469e…` byte-identical before/after — UNTOUCHED. Staging
  original READ-only. Test copy deleted. No cut-over. No commit.**

---

## 2026-06-18 — Post-migration seam fixes (re-analysis `.analysis/20260618-121601/`)

Fixed the seams the v2-sanctum migration introduced/left, per the re-analysis
findings. Scope was strictly `src/skills/suno-agent-band-manager/`; no root
`CLAUDE.md`/`AGENTS.md`, no other skills, no live sidecar writes, no commit.

### Pinned canonical decisions applied (resolves the contract-drift theme)

- **Dynamic-menu key is `menu_text`** — `pre-activate.py` now emits `menu_text`
  (was `menu`); SKILL.md / activation.md already said `{menu_text}`.
- **The always-loaded rebirth set is exactly 7 files** — `access-boundaries.md`,
  `INDEX.md`, `MEMORY.md`, `CREED.md`, `PERSONA.md`, `BOND.md`, `CAPABILITIES.md`.
  `pre-activate.py`'s `SANCTUM_LOAD_ORDER` and every doc that lists the set agree.
- **The loaded/on-demand/not-loaded TIER MAP is owned canonically by the sanctum
  `INDEX.md`** (`assets/INDEX-template.md`). SKILL.md / memory-system.md /
  capabilities.md now POINT at INDEX rather than duplicate the full table.
- **In-prompt runner convention** — `python3 scripts/<name>.py`; scripts with
  non-stdlib PEP 723 deps append "(or `uv run` if deps are missing)". Applied to
  the PULSE template (its `uv run` → this convention) and the prompt invocations.

### THEME 1 (HIGH) — fresh-birth scaffolder ≠ migration output

- **New shared module `scripts/_sanctum_seed.py`** — single source of the
  `shard_creed()` slicing + access-boundaries seeding. BOTH
  `migrate-sidecar-to-v2.py` and `init-sanctum.py` import it (migrate's inline
  `shard_creed`/`SHARD_ROUTING` removed; it now imports them). The module also
  exposes a small recovery CLI (`--out`/`--skill-path`) for re-seeding a missing
  shard — keeps it argparse/JSON/exit-code clean for the script scanner.
- **New `assets/ACCESS-BOUNDARIES-template.md`** — seeded from the live
  `access-boundaries.md` (READ-ONLY), machine-specific bits generalized to
  `{project_root}` + `{username}` and the owner-name made generic. Carries the
  load-bearing PROPORTIONATE write discipline verbatim.
- **`init-sanctum.py` now writes the full set on a fresh birth** —
  access-boundaries.md (from the template) + the 3 creed shards +
  creed-incident-log.md (via the shared module). A born sanctum and a migrated
  sanctum now converge on the same file shape.
- **Fresh-birth history is honest** — `fresh_birth_history()` strips the seeded
  "First Breath / sanctum migration" / "Reborn … memories migrated" lines from
  MEMORY/PERSONA on a fresh birth (no fabricated history; the migrated path keeps
  its accurate framing).
- **Acceptance test added** (`test-init-sanctum.py`): a temp-dir scaffold asserts
  all 7 always-loaded files + the 3 creed shards + creed-incident-log exist and
  are non-empty, access-boundaries substitutes `{project_root}`, and the history
  carries no migration fiction. **PASS** — scaffolded set verified:
  `[access-boundaries, INDEX, MEMORY, CREED, PERSONA, BOND, CAPABILITIES] +
  [creed-disciplines, creed-workshop-capture, creed-package-assembly,
  creed-incident-log]`.

### THEME 3 (HIGH-opp) — agent_type relabel

- `customize.toml`: `agent_type = "memory"` → `"autonomous"` with the comment
  "autonomous (memory + PULSE)". One string + comment; the rest of the surface
  was already correct.

### THEME 4 (MEDIUM) — headless contract completeness

- **`refine-song.md` gains a Headless Mode block** (input/output/blocked contract
  mirroring create-song's) — marked `Headless-eligible: true`.
- **Interactive-only capabilities marked `Headless-eligible: false`** —
  browse-songbook, save-memory, reconcile each declare the marker + the
  `{status: blocked, reason: "interactive-only"}` contract.
- **`activation.md` Headless Activation surfaces the marker** — the router checks
  `Headless-eligible` first, runs the capability's own contract when true, returns
  `blocked: interactive-only` when false (no improvising).
- **Nested-Agent resolution paragraph added** — when Mac is ALREADY headless/
  isolated, the pipeline skills are invoked DIRECTLY via the Skill tool rather
  than nesting Agent-within-Agent (the isolation requirement is already satisfied
  by the outer context; the requirement to run the pipeline at all is unchanged).

### THEME 5 (MEDIUM/HIGH) — WIP-lifecycle determinism → script

- **New `scripts/scan-wip-status.py`** — globs `docs/wip-*.md`, regex-matches the
  `## STATUS: COMPLETED` marker, correlates unmarked WIPs against published
  songbook titles, emits per file
  `{path, status, completed_as, published_date, songbook_ref, correlation_warning}`.
  Reuses `validate-sidecar.py`'s songbook parser (`load_all_songs`/`Song`) via a
  by-path import (registered in `sys.modules` so its `@dataclass` resolves).
  PEP 723 (pyyaml), `--help`, JSON/text output, tests added.
- **Wired into the prompts that hand-scanned WIPs** — `save-memory.md` (WIP
  completion scan + companion-files WIP check), `reconcile.md` (listing
  discipline), `create-song.md` Step 7. The script does the scan/match/correlate;
  the LLM keeps the judgment (confirm the match, authorize the marker write).
  Marker-writing stays with the LLM/normal flow.

### THEME 6 (LOW) — cross-file contract drift

- `pre-activate.py`: emits `menu_text` (was `menu`); `SANCTUM_LOAD_ORDER` extended
  to the canonical 7 (added BOND.md + CAPABILITIES.md); tests updated/added.
- 5-vs-7 rebirth count reconciled in SKILL.md, activation.md, memory-system.md,
  INDEX-template (already 7), pre-activate.py — all now state exactly 7.
- Duplicated tier-maps in SKILL.md / memory-system.md / capabilities.md replaced
  with pointers to `INDEX.md` as canonical.
- In-prompt script runner standardized (incl. PULSE template `uv run` →
  `python3 … (or uv run if deps are missing)`).
- `genre-coverage.py` is now a clean no-op on an empty catalog (missing
  `docs/songbook/` → exit 0 with zeroed `{"bands": [], "band_count": 0}` JSON,
  instead of `sys.exit(2)`). `main()` returns an int; `sys.exit(main())` at the
  bottom keeps the exit-code contract. Tests added.

### Validation

- `scan-path-standards.py`: **10 live findings** = 7 accepted baseline
  (4 INDEX/PULSE-template `{project_root}/_bmad` substitution targets + 3 cross-
  skill `../` links in README/SUNO-REFERENCE/creed) **+ 3 of the SAME accepted
  `bare-bmad` intrinsic category** from the new, necessary
  `ACCESS-BOUNDARIES-template.md` (a Dominion contract MUST name the sanctum
  `{project_root}/_bmad/...` path; identical pattern to the accepted INDEX/PULSE
  template findings). No NEW category of finding; no findings in any
  hand-edited prose file. `(.analysis/` + `.decision-log.md` path-like strings
  excluded as scan noise, per the established baseline convention.)
- `scan-scripts.py`: **status pass, 0 findings** (the new `_sanctum_seed.py` and
  `scan-wip-status.py` carry argparse/`--help`/JSON/exit-codes + unit tests).
- Full suite: **117 passed** via
  `uv run --with pytest --with pyyaml python -m pytest src/skills/suno-agent-band-manager -q -p no:cacheprovider`
  (was 92; +25 new tests across init-sanctum acceptance, scan-wip-status,
  _sanctum_seed, pre-activate menu_text/7-file, genre-coverage empty-catalog).
- **LIVE sidecar UNTOUCHED** — all scaffolding/migration test runs went to TEMP
  dirs; `git status _bmad/ docs/` clean. No cut-over, no commit.
