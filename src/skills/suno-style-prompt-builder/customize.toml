# Customization surface for suno-style-prompt-builder.
# Teams override via {project-root}/_bmad/custom/suno-style-prompt-builder.toml
# Users override via {project-root}/_bmad/custom/suno-style-prompt-builder.user.toml
# Merge order: this base -> team -> user. Scalars override; plain arrays append.

[workflow]
activation_steps_prepend = []
activation_steps_append = []
persistent_facts = ["file:{project-root}/**/project-context.md"]
